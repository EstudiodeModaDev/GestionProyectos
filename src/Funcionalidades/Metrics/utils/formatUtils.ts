/**
 * Formatea un número como porcentaje entero.
 * @param value - Valor numérico a formatear.
 * @returns Porcentaje redondeado en formato texto.
 */
export const formatPercent = (value: number) => `${Math.round(value)}%`;
